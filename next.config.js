/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig
```

4. Πάτα **"Commit changes..."** → **"Commit changes"**

---

## Αρχείο 3: pages/index.js

1. Πάτα **"Add file"** → **"Create new file"**

2. Όνομα αρχείου (ΠΡΟΣΟΧΗ με το `/`):
```
pages/index.js 
